{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: open and save dialogs only. All file reads and writes happen in Rust commands on user-picked paths (review fix 2026-08-30: the fs plugin was unused and its **/*.pdf scope was wider than D9).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
